# APA@POS runtime dependencies
# Install with: pip install -r requirements.txt

python-barcode[images]   # Code128 PNG generation (fallback built-in if absent)
Pillow                   # required by python-barcode ImageWriter
reportlab                # optional — PDF receipts (plain .txt used if absent)

# Tkinter and sqlite3 ship with the Python standard library.
# On macOS install python-tk separately:  brew install python-tk@3.12
# On Windows use the python.org installer (Tk included).
